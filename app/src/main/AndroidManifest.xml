<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.oloralibro">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".activities.ActivityMenu"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ActivityInfoActividades" />
        <activity android:name=".activities.ActivityInfoLibreria" />
        <activity android:name=".activities.ActivityIniciarSesion" />
        <activity android:name=".activities.ActivityRegistrarse" />
        <activity android:name=".activities.ActivityHistorialActividades" />
        <activity android:name=".activities.ActivityListaLibrerias" />
        <activity android:name=".activities.ActivityListaActividades" />
        <activity android:name=".activities.ActivityListaPremios" />
        <activity android:name=".activities.ActivityConfiguracion" />
        <activity android:name=".activities.ActivitySobreNosotros"></activity>
    </application>

</manifest>